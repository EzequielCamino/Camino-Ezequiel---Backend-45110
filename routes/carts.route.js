/* const { Router } = require("express");
const fs = require('fs');
const route = Router();
const CartManager = require('../CartManager')
const cartManager = new CartManager('../carts.json')

route.post("", async (req, res) => {
    const response = await cartManager()
}) */